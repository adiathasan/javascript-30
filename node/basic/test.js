const idk = (toCheck = [], given = []) => {
	const main = [];
	let counter = 0;
	given.forEach((item) => {});
};

const array1 = ['abc', 'abc', 'xyz', 'cde', 'uvw'];
const array2 = ['abc', 'cde', 'uap'];

idk(array1, array2);
